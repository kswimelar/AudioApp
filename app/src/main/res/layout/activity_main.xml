<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">
    <Button
            android:text="Play"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/btnPlay" android:onClick="playAudio"
            app:layout_constraintTop_toTopOf="parent" app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="101dp" app:layout_constraintEnd_toEndOf="parent"/>
    <Button
            android:text="Record"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/btnRecord" android:onClick="recordAudio"
            app:layout_constraintStart_toStartOf="@+id/btnPlay" android:layout_marginTop="53dp"
            app:layout_constraintTop_toBottomOf="@+id/btnPlay"/>
    <Button
            android:text="Stop"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/btnStop" android:onClick="stopAudio"
            app:layout_constraintStart_toStartOf="@+id/btnRecord" android:layout_marginTop="58dp"
            app:layout_constraintTop_toBottomOf="@+id/btnRecord"/>
</android.support.constraint.ConstraintLayout>